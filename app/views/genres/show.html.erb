<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @genre.name %>
</p>

<p>
  <strong>Artists in Genre</strong>
  <% @genre.artists.each do |artist| %>

    <ul>
      <li> <%= artist.name %> </li>
    </ul>
    <% end %>
</p>


<%= link_to 'Edit', edit_genre_path(@genre) %> |
<%= link_to 'Back', genres_path %>
